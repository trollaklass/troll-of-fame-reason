(library
  (name lib)
  (flags (-w -40 -w +26))
  (public_name troll-of-fame)
  (libraries console.lib pastel.lib)
  (preprocess (pps ppx_deriving.std ppx_deriving.enum ppx_deriving.show ppx_deriving.eq ppx_deriving.ord))
  )